[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
extend-select = ["E402"]
ignore = [
    "B008", # FastAPI dependency injection intentionally uses Depends in defaults.
]

[tool.ruff.lint.per-file-ignores]
"migrations/versions/20260916_0001_auth_foundation.py" = ["I001"]
